[project]
name = "email-manager"
type = "package"
description = "Template-first email sending library for NextNode projects"

[scripts]
lint = "lint"
test = "test"
build = "build"

[package]
scope = "@nextnode-solutions"
access = "public"
canary_on_label = true
