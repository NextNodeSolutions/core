---
interface Props {
	message: string
}

const { message } = Astro.props
---

<div class="mt-8 rounded-2xl border border-dashed border-base-200 bg-white p-10 text-center">
	<p class="font-mono text-xs text-base-500">{message}</p>
</div>
